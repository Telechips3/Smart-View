# 1. 컴파일된 바이너리 및 오브젝트 파일 (절대 올리면 안 됨)
*.o
*.ko
*.obj
*.elf

# 2. 커널 모듈 빌드 시스템이 만드는 잡동사니
*.mod
*.mod.c
*.mod.o
modules.order
Module.symvers
Module.markers

# 3. 숨겨진 컴파일 명령 파일 (이게 진짜 많이 생김)
.*.cmd
.*.o.d

# 4. 임시 폴더
.tmp_versions/

# 5. 실행 파일 (테스트용 C 파일 등을 컴파일했을 때)
# (혹시 test_app 같은 걸 만든다면 추가하세요)
# test_app
# a.out

# 6. 편집기 임시 파일 (Vim, VSCode 등)
*.swp
*~
.vscode/

# --- [컴파일 실행 파일 및 바이너리] ---
# 특정 이름이 없는 리눅스 실행 파일(보통 확장자 없음)을 제외하기 위해 
# 먼저 모든 파일을 제외하고 확장자가 있는 파일만 다시 포함하는 방식을 쓰기도 하지만,
# 보통은 빌드 폴더를 통째로 제외하는 것이 가장 깔끔합니다.

# 빌드 결과물 폴더 (cmake 사용 시)
build/
bin/
out/

# 확장자가 없는 실행 파일들을 개별적으로 기록하거나, 
# 프로젝트 실행 파일 이름을 직접 추가하세요 (예: my_lidar_app)
# 아래는 일반적인 실행 파일 이름 예시입니다.
a.out
*.out
*.app

# --- [오브젝트 및 라이브러리 파일] ---
*.o
*.obj
*.a
*.la
*.lo
*.so
*.so.*
*.dll
*.dyl


# --- [IDE 및 에디터 관련] ---
.vscode/
.idea/
*.swp
*~

# --- [로그 및 임시 파일] ---
*.log
*.tmp
