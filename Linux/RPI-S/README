Requirements
* Raspberry Pi 5
* Active Cooler
* Hailo AI HAT+ (26 TOPS)

Hailo 설치
```
sudo apt update
sudo apt install hailo-all
sudo reboot
```

가상환경 생성 & 활성화
```
python3 -m venv --system-site-packages hailo_env
source hailo_env/bin/activate
```

기본 라이브러리
```
pip install numpy opencv-python
```


모델 확인:

ls /usr/share/hailo-models/
yolov8s_h8.hef 존재 확인

추가 설치
```
pip install --upgrade pip setuptools wheel
pip install "numpy>=1.26.0" "pandas>=2.0.0" "scikit-learn>=1.3.0"
pip install boxmot --no-deps
pip install loguru filterpy lapx gdown gitpython ftfy regex yacs scikit-image
pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
pip install ultralytics
```
