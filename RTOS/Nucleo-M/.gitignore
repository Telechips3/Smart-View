# ---------------------------------
# STM32CubeIDE Ignore Rules
# ---------------------------------

# 1. 빌드 결과물 폴더 (가장 중요: 용량 큼, 매번 바뀜)
Debug/
Release/
build/

# 2. 컴파일 중간 파일 및 바이너리
*.elf
*.bin
*.hex
*.map
*.list
*.o
*.d
*.su
*.srec

# 3. CubeMX/IDE 자동 생성 메타데이터 (ioc가 있으면 다시 생성됨)
.mxproject

# 4. (선택) IDE 설정 폴더
# 팀원 간 IDE 설정(폰트, 경로 등)을 강제로 맞추려면 주석 처리(#) 하세요.
# 보통은 각자 설정이 다르므로 무시하는 것이 좋습니다.
.settings/

# ---------------------------------
# 주의: 아래 파일들은 절대 지우면 안 됨! (프로젝트 핵심 파일)
# .project
# .cproject
# *.ioc
# ---------------------------------